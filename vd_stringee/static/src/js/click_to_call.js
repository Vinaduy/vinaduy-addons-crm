/** @odoo-module **/
/**
 * (Trước đây file này register systray dialer + global floating indicator.)
 *
 * User feedback: KHÔNG cần button thứ 2. Chỉ giữ nút FAB form (vd_call_status
 * widget trong vd_crm_lead) — nó tự morph xanh → cam → đỏ + chữ + timer.
 *
 * File này giữ trống chỉ để bundle không vỡ. Không register gì.
 */
